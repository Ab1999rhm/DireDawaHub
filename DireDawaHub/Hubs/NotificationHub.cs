using Microsoft.AspNetCore.SignalR;

namespace DireDawaHub.Hubs;

public class NotificationHub : Hub
{
    // Clients will automatically connect to this bridge via JavaScript
    // No additional server-side state is required for a simple broadcast
}
